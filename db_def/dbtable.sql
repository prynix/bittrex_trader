CREATE TABLE (?) (
    currency varchar,
    last decimal(30,20),
    bid decimal(30,20),
    ask decimal(30,20),
    volume decimal(30,20),
    openbuyorders decimal(30,20),
    opensellorders  decimal(30,20),
    change decimal(30,20),
    date datetime
);

CREATE TABLE (?) (currency varchar, last decimal(30,20), bid decimal(30,20), ask decimal(30,20), volume decimal(30,20), openbuyorders decimal(30,20), opensellorders  decimal(30,20), change decimal(30,20), date datetime);

CREATE TABLE (?) (currency varchar, last decimal, bid decimal, ask decimal, volume decimal, openbuyorders decimal, opensellorders  decimal, change decimal, date datetime);
