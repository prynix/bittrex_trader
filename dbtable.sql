CREATE TABLE (?) (
    currency varchar,
    last decimal(18,12),
    bid decimal(18,12),
    ask decimal(18,12),
    volume decimal(18,12),
    openbuyorders decimal(18,12),
    opensellorders  decimal(18,12),
    change decimal(18,12),
    date datetime
);

CREATE TABLE (?) (currency varchar, last decimal(18,12), bid decimal(18,12), ask decimal(18,12), volume decimal(18,12), openbuyorders decimal(18,12), opensellorders  decimal(18,12), change decimal(18,12), date datetime);
